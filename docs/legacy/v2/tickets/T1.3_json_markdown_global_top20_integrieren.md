# T1.3 – JSON/Markdown: global_top20 integrieren

**Quelle:** `docs/v2/30_IMPLEMENTATION_TICKETS.md` (Canonical, Stand: 2026-02-18)  
**Erstellt am:** 2026-02-19

## Ziel

Erweitere JSON- und Markdown-Outputs um `global_top20` (Top-20 des Global Rankings).

## Anforderungen (aus Canonical)
- `global_top20` integrieren

## Akzeptanzkriterien
- JSON enthält `global_top20` mit deterministischer Reihenfolge.
- Markdown enthält Abschnitt „Global Top 20“.
- Output bleibt kompatibel (keine stillen Feldänderungen ohne Versionierung).

## Relevante Dateien / Einstiegspunkte
- JSON-Serializer / Schema
- Markdown-Renderer/Templates
- `docs/SCHEMA_CHANGES.md` (falls schema_version bump nötig)

## Vorgehen
1. `global_top20` in JSON integrieren.
2. Markdown-Section ergänzen.
3. Tests/Golden-Fixtures aktualisieren.
4. Bei Schema-Änderungen: `schema_version` bump + Eintrag in `docs/SCHEMA_CHANGES.md`.


## Abschluss- und Archiv-Schritt (Pflicht)
Nach Merge/Abschluss dieses Tickets:
1. Verschiebe **diese** Ticket-Datei nach `docs/legacy/tickets/` (gleicher Dateiname).
2. Falls du in `docs/v2/30_IMPLEMENTATION_TICKETS.md` einen Status-Abschnitt führst: Ticket dort als „done“ markieren (optional, nur wenn ihr das so nutzt).
