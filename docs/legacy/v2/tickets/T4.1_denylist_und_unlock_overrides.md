# T4.1 – denylist/unlock_overrides

**Quelle:** `docs/v2/30_IMPLEMENTATION_TICKETS.md` (Canonical, Stand: 2026-02-18)  
**Erstellt am:** 2026-02-19

## Ziel

Implementiere Risk Flags: denylist und unlock_overrides.

## Anforderungen (aus Canonical)
- Denylist hard exclude
- major unlock innerhalb 14d hard exclude; minor unlock soft penalty

## Akzeptanzkriterien
- Denylist wirkt als Hard Exclude (Setup + Global).
- Unlock-Regeln korrekt (Zeitfenster 14 Tage).
- Soft-Penalty ist spec-konform (z. B. separater Sort-Key / Adjustment).
- Tests für denylist, major/minor unlock.

## Relevante Dateien
- Risk/Filter Layer
- Config/Overrides Daten
- Ranking/Scoring

## Vorgehen
1. Datenformat/Quelle definieren.
2. Hard-Exclude Flags implementieren.
3. Soft-Penalty implementieren.
4. Tests.


## Abschluss- und Archiv-Schritt (Pflicht)
Nach Merge/Abschluss dieses Tickets:
1. Verschiebe **diese** Ticket-Datei nach `docs/legacy/tickets/` (gleicher Dateiname).
2. Falls du in `docs/v2/30_IMPLEMENTATION_TICKETS.md` einen Status-Abschnitt führst: Ticket dort als „done“ markieren (optional, nur wenn ihr das so nutzt).
