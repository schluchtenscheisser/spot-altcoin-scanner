# T2.1 – Proxy-Liquidity Score

**Quelle:** `docs/v2/30_IMPLEMENTATION_TICKETS.md` (Canonical, Stand: 2026-02-18)  
**Erstellt am:** 2026-02-19

## Ziel

Implementiere einen Proxy-Liquiditäts-Score als percent-rank auf Basis von `quote_volume_24h`.

## Anforderungen (aus Canonical)
- Proxy: `quote_volume_24h`
- `proxy_liquidity_score` als percent_rank (log optional)

## Akzeptanzkriterien
- Proxy-Wert wird pro Symbol geladen, sofern verfügbar.
- `proxy_liquidity_score` = percent-rank über definierte Population.
- Optional log-transform nur spec-/config-konform.
- Tests: fehlende Werte, deterministische Ranks, korrekte Population.

## Relevante Dateien
- Daten-Loader für `quote_volume_24h`
- Feature/Scoring Pipeline
- `percent_rank` Implementierung (siehe Epic 3)

## Vorgehen
1. Quelle für `quote_volume_24h` lokalisieren.
2. Feature berechnen (inkl. optionalem log).
3. percent-rank berechnen.
4. Output-Feld hinzufügen + Tests.


## Abschluss- und Archiv-Schritt (Pflicht)
Nach Merge/Abschluss dieses Tickets:
1. Verschiebe **diese** Ticket-Datei nach `docs/legacy/tickets/` (gleicher Dateiname).
2. Falls du in `docs/v2/30_IMPLEMENTATION_TICKETS.md` einen Status-Abschnitt führst: Ticket dort als „done“ markieren (optional, nur wenn ihr das so nutzt).
