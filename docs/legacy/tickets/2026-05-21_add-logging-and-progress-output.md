````text
# Codex Task: Add Logging and Progress Output to Historical Replay Runner\n\n## Problem\n\nThe historical replay runner currently provides no operational progress output and no contextual logging around symbol/day failures. When running the full historical replay in Codespace, it is difficult to tell whether the process is progressing, hanging, or failing on a specific symbol/day.\n\nAdd structured logging and progress output without changing replay semantics.\n\n## What to implement\n\n### 1. Use Python standard logging module\n\nIn `scanner/evaluation/historical_replay/replay_runner.py`, add:\n\n```python\nimport logging\nlogger = logging.getLogger(__name__)\n````\n\nIn `scanner/tools/run_historical_daily_replay.py`, configure logging before calling `run_replay`:\n\n```python\nimport logging\nimport time\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format="%(asctime)s %(levelname)s %(name)s: %(message)s",\n    datefmt="%Y-%m-%dT%H:%M:%SZ",\n)\nlogging.Formatter.converter = time.gmtime\n```\n\nUse UTC timestamps in logs.\n\n### 2. Required log statements in replay_runner.py\n\nAt replay start:\n\n```text\nINFO: Starting replay scenario_id=... replay_id=... symbols=N evaluation=2025-05-01..2026-05-17\n```\n\nBefore each daily bar:\n\n```text\nINFO: Replaying day 2025-05-01 (1/382) symbols=433\n```\n\nAfter each day completes:\n\n```text\nINFO: Day 2025-05-01 done: admitted=N untracked=N warmup_skipped=N missing_data=N events=N diagnostics=N\n```\n\nEvery 10 days:\n\n```text\nINFO: Progress 10/382 days completed, signal_events_so_far=N diagnostics_so_far=N\n```\n\nBefore writing outputs:\n\n```text\nINFO: Writing replay outputs run_dir=...\n```\n\nAfter output files are written:\n\n```text\nINFO: Wrote replay outputs diagnostics=... events=... manifest=...\n```\n\nAt replay completion:\n\n```text\nINFO: Replay complete replay_id=... days=N symbols_total=N symbols_evaluable=N signal_events=N diagnostics=N\n```\n\n### 3. Exception logging\n\nOn exception during symbol processing, log context with traceback:\n\n```text\nERROR: Symbol BTCUSDT day 2025-05-01 failed: <exception message>\n```\n\nUse `logger.exception(...)`.\n\nDo not swallow exceptions. After logging, re-raise so the CLI exits non-zero and the full traceback/context is visible.\n\nOn exception that aborts the entire replay, log:\n\n```text\nCRITICAL: Replay aborted after N/N days: <exception message>\n```\n\nUse `logger.critical(..., exc_info=True)` or `logger.exception(...)`, then re-raise.\n\nImplementation guidance:\n\n* Wrap the main replay body in a narrow try/except that logs abort context and re-raises.\n* Wrap per-symbol processing only to add symbol/day context and re-raise.\n* Do not use bare `except:`.\n* Do not use `except Exception: pass`.\n* Do not continue after a symbol failure unless the existing semantics explicitly support that. For now, fail fast with context.\n\n### 4. Progress counters\n\nTrack daily counters inside the day loop:\n\n```text\nadmitted_count\nuntracked_count\nwarmup_skipped_count\nmissing_data_count\nevents_today\ndiagnostics_today\n```\n\nCounting rules:\n\n* `admitted_count`: disposition_status == admitted\n* `untracked_count`: disposition_status == untracked\n* `warmup_skipped_count`: symbols skipped due to warm-up, even though no diagnostics row is emitted\n* `missing_data_count`: disposition_status == not_evaluable_missing_data\n* `events_today`: events emitted on that day\n* `diagnostics_today`: diagnostics rows emitted on that day\n\nAlso track cumulative:\n\n```text\ndays_completed\nsignal_events_so_far\ndiagnostics_so_far\n```\n\n### 5. Add the scenario YAML to the repo\n\nAdd:\n\n```text\nconfigs/replay_scenarios/hsq_replay_2025_05_to_2026_05_v1.yml\n```\n\nContent:\n\n```yaml\nscenario_id: hsq_replay_2025_05_to_2026_05_v1\n\nhistory_dataset_ref: snapshots/history/ohlcv\nhistory_manifest_ref: snapshots/history/manifests/history_manifest.json\nuniverse_manifest_ref: snapshots/history/manifests/universe_manifest.json\n\ntimeframes:\n  - 1d\n  - 4h\n\nuniverse_mode: binance_spot_usdt_all\n\nevaluation:\n  start_date: "2025-05-01"\n  end_date: "2026-05-17"\n\nexecution:\n  mode: disabled_historical_ohlcv_only\n\nscanner_config:\n  ref: config/config.yml\n  hash: "8940a32173b4975ee635933981566719fb6d0225b34f4080a331a9fce4000594"\n\nregime_labels:\n  method_ref: snapshots/history/regime_labels/regime_labels_btc_weekly_30d_return_vol_v1.json\n\ndaily_replay_time_policy:\n  settlement_delay_seconds: 0\n\nwarmup:\n  warm_up_1d_bars: 120\n  warm_up_4h_bars: 120\n\nsplits:\n  calibration:\n    start_date: "2025-05-01"\n    end_date: "2026-01-31"\n  validation:\n    start_date: "2026-02-01"\n    end_date: "2026-05-17"\n```\n\nDo not invent or alter the scanner_config.hash. Use exactly the value above.\n\n### 6. Files to change\n\n* `scanner/evaluation/historical_replay/replay_runner.py`\n* `scanner/tools/run_historical_daily_replay.py`\n* `configs/replay_scenarios/hsq_replay_2025_05_to_2026_05_v1.yml`\n\n### 7. Do not change\n\n* `scanner/evaluation/historical_replay/bar_loader.py`\n* `scanner/evaluation/historical_replay/scenario.py`\n* `scanner/evaluation/historical_replay/scenario_registry.py`\n* Pre-1 fetch logic\n* Live scanner modules\n* Replay semantics\n* Bucket mapping\n* Output schemas except for no-op-safe logging side effects\n\n### 8. Tests\n\nAdd or update focused tests if practical:\n\n* CLI configures logging and calls `run_replay`.\n* replay runner logs replay start and day progress on a small fixture.\n* per-symbol exception logs symbol/day context and re-raises.\n* abort-level exception logs replay abort context and re-raises.\n\nDo not require a full real replay in tests.\n\n### 9. Report\n\nAfter implementation, report:\n\n* files changed,\n* whether replay semantics were unchanged,\n* example log output for the first 3 days,\n* pytest result for focused tests.\n\n````````text\n# Codex Task: Add Logging and Progress Output to Historical Replay Runner\n\n## Problem\n\nThe historical replay runner currently provides no operational progress output and no contextual logging around symbol/day failures. When running the full historical replay in Codespace, it is difficult to tell whether the process is progressing, hanging, or failing on a specific symbol/day.\n\nAdd structured logging and progress output without changing replay semantics.\n\n## What to implement\n\n### 1. Use Python standard logging module\n\nIn `scanner/evaluation/historical_replay/replay_runner.py`, add:\n\n```python\nimport logging\nlogger = logging.getLogger(__name__)\n````\n\nIn `scanner/tools/run_historical_daily_replay.py`, configure logging before calling `run_replay`:\n\n```python\nimport logging\nimport time\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format="%(asctime)s %(levelname)s %(name)s: %(message)s",\n    datefmt="%Y-%m-%dT%H:%M:%SZ",\n)\nlogging.Formatter.converter = time.gmtime\n```\n\nUse UTC timestamps in logs.\n\n### 2. Required log statements in replay_runner.py\n\nAt replay start:\n\n```text\nINFO: Starting replay scenario_id=... replay_id=... symbols=N evaluation=2025-05-01..2026-05-17\n```\n\nBefore each daily bar:\n\n```text\nINFO: Replaying day 2025-05-01 (1/382) symbols=433\n```\n\nAfter each day completes:\n\n```text\nINFO: Day 2025-05-01 done: admitted=N untracked=N warmup_skipped=N missing_data=N events=N diagnostics=N\n```\n\nEvery 10 days:\n\n```text\nINFO: Progress 10/382 days completed, signal_events_so_far=N diagnostics_so_far=N\n```\n\nBefore writing outputs:\n\n```text\nINFO: Writing replay outputs run_dir=...\n```\n\nAfter output files are written:\n\n```text\nINFO: Wrote replay outputs diagnostics=... events=... manifest=...\n```\n\nAt replay completion:\n\n```text\nINFO: Replay complete replay_id=... days=N symbols_total=N symbols_evaluable=N signal_events=N diagnostics=N\n```\n\n### 3. Exception logging\n\nOn exception during symbol processing, log context with traceback:\n\n```text\nERROR: Symbol BTCUSDT day 2025-05-01 failed: <exception message>\n```\n\nUse `logger.exception(...)`.\n\nDo not swallow exceptions. After logging, re-raise so the CLI exits non-zero and the full traceback/context is visible.\n\nOn exception that aborts the entire replay, log:\n\n```text\nCRITICAL: Replay aborted after N/N days: <exception message>\n```\n\nUse `logger.critical(..., exc_info=True)` or `logger.exception(...)`, then re-raise.\n\nImplementation guidance:\n\n* Wrap the main replay body in a narrow try/except that logs abort context and re-raises.\n* Wrap per-symbol processing only to add symbol/day context and re-raise.\n* Do not use bare `except:`.\n* Do not use `except Exception: pass`.\n* Do not continue after a symbol failure unless the existing semantics explicitly support that. For now, fail fast with context.\n\n### 4. Progress counters\n\nTrack daily counters inside the day loop:\n\n```text\nadmitted_count\nuntracked_count\nwarmup_skipped_count\nmissing_data_count\nevents_today\ndiagnostics_today\n```\n\nCounting rules:\n\n* `admitted_count`: disposition_status == admitted\n* `untracked_count`: disposition_status == untracked\n* `warmup_skipped_count`: symbols skipped due to warm-up, even though no diagnostics row is emitted\n* `missing_data_count`: disposition_status == not_evaluable_missing_data\n* `events_today`: events emitted on that day\n* `diagnostics_today`: diagnostics rows emitted on that day\n\nAlso track cumulative:\n\n```text\ndays_completed\nsignal_events_so_far\ndiagnostics_so_far\n```\n\n### 5. Add the scenario YAML to the repo\n\nAdd:\n\n```text\nconfigs/replay_scenarios/hsq_replay_2025_05_to_2026_05_v1.yml\n```\n\nContent:\n\n```yaml\nscenario_id: hsq_replay_2025_05_to_2026_05_v1\n\nhistory_dataset_ref: snapshots/history/ohlcv\nhistory_manifest_ref: snapshots/history/manifests/history_manifest.json\nuniverse_manifest_ref: snapshots/history/manifests/universe_manifest.json\n\ntimeframes:\n  - 1d\n  - 4h\n\nuniverse_mode: binance_spot_usdt_all\n\nevaluation:\n  start_date: "2025-05-01"\n  end_date: "2026-05-17"\n\nexecution:\n  mode: disabled_historical_ohlcv_only\n\nscanner_config:\n  ref: config/config.yml\n  hash: "8940a32173b4975ee635933981566719fb6d0225b34f4080a331a9fce4000594"\n\nregime_labels:\n  method_ref: snapshots/history/regime_labels/regime_labels_btc_weekly_30d_return_vol_v1.json\n\ndaily_replay_time_policy:\n  settlement_delay_seconds: 0\n\nwarmup:\n  warm_up_1d_bars: 120\n  warm_up_4h_bars: 120\n\nsplits:\n  calibration:\n    start_date: "2025-05-01"\n    end_date: "2026-01-31"\n  validation:\n    start_date: "2026-02-01"\n    end_date: "2026-05-17"\n```\n\nDo not invent or alter the scanner_config.hash. Use exactly the value above.\n\n### 6. Files to change\n\n* `scanner/evaluation/historical_replay/replay_runner.py`\n* `scanner/tools/run_historical_daily_replay.py`\n* `configs/replay_scenarios/hsq_replay_2025_05_to_2026_05_v1.yml`\n\n### 7. Do not change\n\n* `scanner/evaluation/historical_replay/bar_loader.py`\n* `scanner/evaluation/historical_replay/scenario.py`\n* `scanner/evaluation/historical_replay/scenario_registry.py`\n* Pre-1 fetch logic\n* Live scanner modules\n* Replay semantics\n* Bucket mapping\n* Output schemas except for no-op-safe logging side effects\n\n### 8. Tests\n\nAdd or update focused tests if practical:\n\n* CLI configures logging and calls `run_replay`.\n* replay runner logs replay start and day progress on a small fixture.\n* per-symbol exception logs symbol/day context and re-raises.\n* abort-level exception logs replay abort context and re-raises.\n\nDo not require a full real replay in tests.\n\n### 9. Report\n\nAfter implementation, report:\n\n* files changed,\n* whether replay semantics were unchanged,\n* example log output for the first 3 days,\n* pytest result for focused tests.\n\n````