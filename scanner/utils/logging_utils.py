"""
Logging utilities.

Responsibilities:
- Configure application-wide logging.
- Ensure logs are written to both:
  - console (stdout)
  - log file in logs/scanner.log (if enabled by config).
- Provide a convenient function to obtain a configured logger.
"""

